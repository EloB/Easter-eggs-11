Blocket.se - Last easter egg

Install Node.js from http://nodejs.org/#download

The encryptions is highly optimized with pre-defined index. Because of the native function were quite slow.

Run server:
node app.js

Connect to server
telnet localhost 1337

The last rows in encryption.coffee has commented benchmark code.